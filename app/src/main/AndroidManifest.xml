<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.syncodec.momento">

	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

	<application
		android:name=".Momento"
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher"
		android:supportsRtl="false"
		android:theme="@style/Theme.Momento">

		<meta-data
			android:name="com.google.android.gms.version"
			android:value="@integer/google_play_services_version" />
		<meta-data
			android:name="com.google.android.geo.API_KEY"
			android:value="${mapsApiKey}" />

		<provider
			android:name="androidx.core.content.FileProvider"
			android:authorities="com.syncodec.momento.provider"
			android:exported="false"
			android:grantUriPermissions="true">
			<meta-data
				android:name="android.support.FILE_PROVIDER_PATHS"
				android:resource="@xml/file_provider_path">
			</meta-data>
		</provider>

		<activity
			android:name=".MainActivity"
			android:exported="true"
			android:windowSoftInputMode="adjustResize"
			android:theme="@style/Theme.Momento.NoActionBar">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity
			android:name=".searchComponent.SearchActivity"
			android:exported="false"
			android:theme="@style/Theme.Momento.NoActionBar">
		</activity>

		<activity
			android:name=".todayComponent.TodayActivity"
			android:exported="false"
			android:theme="@style/Theme.Momento.NoActionBar">
		</activity>

		<activity
			android:name=".noteComponent.NoteActivity"
			android:exported="false"
			android:windowSoftInputMode="adjustResize"
			android:theme="@style/Theme.Momento.NoActionBar">
		</activity>

		<activity
			android:name=".bucketComponent.BucketActivity"
			android:exported="false"
			android:windowSoftInputMode="adjustResize"
			android:theme="@style/Theme.Momento.NoActionBar">
		</activity>

		<activity
			android:name=".bucketItemComponent.BucketItemActivity"
			android:exported="false"
			android:windowSoftInputMode="adjustResize"
			android:theme="@style/Theme.Momento.NoActionBar">
		</activity>

		<activity
			android:name=".notebookComponent.NotebookActivity"
			android:exported="false"
			android:windowSoftInputMode="adjustResize"
			android:theme="@style/Theme.Momento.NoActionBar">
		</activity>
	</application>
</manifest>
